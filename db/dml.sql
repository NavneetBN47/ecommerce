-- =========================
-- DML FOR CART MANAGEMENT SYSTEM (LLD as authoritative)
-- =========================
-- Seed data for demonstration, as per LLD
-- No seed carts or cart_items because cart lifecycle is lazy and ephemeral
-- Seed products only if required by LLD (reference only)

-- Example seed product (if allowed)
-- INSERT INTO products (product_id, name, price, stock) VALUES ('11111111-1111-1111-1111-111111111111', 'Sample Product', 10.00, 100);
